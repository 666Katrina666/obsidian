# -*- coding: utf-8 -*-
"""#time_loop - time loops."""
import re

TAG = "time_loop"

PATTERNS = [
    r"временная петля", r"временной петле", r"в петле времени", r"попал в петлю",
    r"зациклен", r"зациклился", r"зацикливание", r"застрял в цикле",
    r"повторяет один день", r"один день повторяется", r"один и тот же день",
    r"опять тот же день", r"день сурка", r"снова и снова", r"повторяется один и тот же",
    r"цикл повторяется", r"вырваться из петли", r"выйти из петли",
    r"time loop", r"живет один день заново", r"проживает один день снова",
]
PAT = re.compile("|".join(PATTERNS), re.IGNORECASE)


def check(text):
    return bool(text and PAT.search(text))
