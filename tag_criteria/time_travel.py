# -*- coding: utf-8 -*-
"""#time_travel - time travel."""
import re

TAG = "time_travel"

PATTERNS = [
    r"путешествие во времени", r"путешественник во времени",
    r"из будущего", r"из прошлого", r"в будущее", r"в прошлое",
    r"вернулся в прошлое", r"попал в прошлое", r"перенесся в прошлое",
    r"перенеслась в прошлое", r"пропал во времени", r"застрял во времени",
    r"исчез во времени", r"машина времени", r"временной парадокс",
    r"из другого времени", r"в другом времени", r"тайм-травел", r"таймтравел",
    r"time travel", r"когда вернулся", r"когда попал в прошлое",
]
PAT = re.compile("|".join(PATTERNS), re.IGNORECASE)


def check(text):
    return bool(text and PAT.search(text))
